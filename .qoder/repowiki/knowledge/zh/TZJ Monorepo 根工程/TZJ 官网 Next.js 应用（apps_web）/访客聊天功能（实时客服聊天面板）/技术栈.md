React Server Components 中的 Client Component（`'use client'`）、Socket.IO 客户端（`socket.io-client`，支持 websocket/polling 双传输）、Next.js 环境变量 `NEXT_PUBLIC_CHAT_API_URL` / `NEXT_PUBLIC_CHAT_SOCKET_URL` 配置后端地址、`@tzj/ui` 提供的 MarkdownPreview（Vditor）与基础 UI 组件。