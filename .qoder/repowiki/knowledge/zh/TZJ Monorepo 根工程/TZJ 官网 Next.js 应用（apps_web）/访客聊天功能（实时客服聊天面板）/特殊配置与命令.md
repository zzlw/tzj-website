需配置 `NEXT_PUBLIC_CHAT_API_URL`（默认 `http://localhost:4000/api/v1`）和 `NEXT_PUBLIC_CHAT_SOCKET_URL`（默认 `http://localhost:4000`）指向 chat-support-service；首次进入会读取 `localStorage` 中 `tzj_chat_visitor` 恢复 roomId/token。