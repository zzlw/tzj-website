- 页面组件统一声明为 async function 并导出 `generateMetadata`，用于在服务端生成 SEO 元数据。
- 文案全部通过 `getTranslations(namespace)` 获取，页面中不出现硬编码字符串。
- 隐私与条款页采用相同模板：`PageHero` + `Container` + `t.raw('sections')` 循环渲染章节，并在底部展示 `contactSection` 与 `lastUpdated`。
- SEO 元数据通过共享工具函数生成（`generateSeo` 或 `createPageMetadata`），而非直接返回对象。