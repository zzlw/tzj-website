Next.js App Router（Route Groups `(dashboard)`）、React Server Components 用于 layout 鉴权、`@tzj/ui`（shadcn 风格组件）、`@tzj/dnd` 的 `SortableTree` 实现拖拽、lucide-react 图标、Vditor.preview 通过 `@tzj/ui` 的 MarkdownPreview 渲染。