Next.js App Router（RSC + Client Components）、`@tzj/ui`（shadcn 风格 Sidebar/ScrollArea/Card 等）、React Query（`QueryProvider`）、JWT 双 Token（access/refresh）存储在 httpOnly Cookie 中，BFF 模式通过 `/api/bff/[...path]` 转发请求到后端 `API_BASE`。