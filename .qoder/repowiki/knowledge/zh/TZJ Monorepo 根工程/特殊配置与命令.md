根目录 pnpm scripts 提供统一入口：`pnpm dev` 启动全部子应用（scripts/dev.mjs 协调），`pnpm dev:web|admin|api` 单独启动某应用；`pnpm db:up/down/logs/clean` 管理 PostgreSQL；`pnpm db:migrate/baseline` 执行 Prisma 迁移；`pnpm deploy:prod|ecs` 触发生产/ECS 部署。