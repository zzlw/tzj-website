本地开发：`pnpm db:up` 或 `docker compose -f infra/docker/docker-compose.dev.yml up -d`；生产部署：云效流水线自动触发，或本地 `./infra/docker/deploy-local.sh`（需先复制 `.env.deploy.local.example` 填写 ACR/ECS 凭据），ECS 上执行 `./infra/docker/deploy.sh all <tag>`。