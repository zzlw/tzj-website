- 每个包采用 src/index.ts 作为唯一导出入口，子模块通过各自 index.ts 聚合后再向上导出。
- 所有包均提供 tsconfig.build.json 与 tsconfig.json 分离的构建配置，继承自 @tzj/config 提供的基线。
- 共享类型集中在 @tzj/types，其他包通过 import 而非复制方式引用，避免类型定义分散。
- UI 组件遵循「一个组件一个目录 + index.ts 重导出」的目录结构，便于按需引入与 tree-shaking。