React 19 + TypeScript；UI 原语基于 @radix-ui/* 系列（alert-dialog、dialog、popover、select、tabs、tooltip 等）；样式系统采用 Tailwind CSS 4 + class-variance-authority + clsx + tailwind-merge；表格使用 @tanstack/react-table；Markdown 渲染使用 react-markdown + rehype-sanitize + remark-gfm，预览使用 Vditor；Toast 使用 Sonner；图片预览使用 react-photo-view；音频播放使用 react-use-audio-player。