- 每个设计令牌先以 `CONSTANT_NAME = '值'` 的形式单独 `export const` 声明，再在 `tokens` 对象中以驼峰键名二次引用，保证 JS/TS 与 CSS 变量命名风格分离。
- 所有令牌值均为字符串字面量，并通过 `as const` 固化到 `tokens` 对象，使 `ThemeTokens` 类型能精确推断出每个属性的具体字符串值。
- 文件内按语义分组（颜色、字体、间距、圆角、阴影、动画）用注释分隔线划分区块，每个常量附带 JSDoc 中文注释说明用途。