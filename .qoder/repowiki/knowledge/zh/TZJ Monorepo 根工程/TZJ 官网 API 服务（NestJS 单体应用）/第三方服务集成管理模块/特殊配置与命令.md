新增集成只需三步：在 `INTEGRATION_REGISTRY` 追加 slug 与字段定义、在 `INTEGRATION_ENV_FALLBACK` 补充环境变量映射（可选）、在 `integration.testers.ts` 添加 `testConnection` 实现；无需修改数据库表结构，Prisma 会按 slug 自动 upsert。部署时必须设置 `SECRETS_ENCRYPTION_KEY`（≥32 字符）才能保存加密凭证。