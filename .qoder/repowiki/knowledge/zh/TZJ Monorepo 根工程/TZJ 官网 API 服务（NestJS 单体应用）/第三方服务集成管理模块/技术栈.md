NestJS 模块架构；Prisma ORM 进行 `integrations` 表的 upsert 操作；Zod 做请求体校验；阿里云官方 SDK（`@alicloud/captcha20230305`、`@alicloud/dm20151123`、`@alicloud/openapi-core`）；AES-256-GCM 对称加密（通过 `../common/crypto/secrets-crypto` 的 `encryptSecrets`/`decryptSecrets`）存储敏感凭据；`SECRETS_ENCRYPTION_KEY` 环境变量至少 32 字符才允许保存。