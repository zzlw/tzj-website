模块启动时通过 `onModuleInit` 自动执行 `syncSystemRoles`，将 `SYSTEM_ROLE_SLUGS`（当前为 ADMIN）写入 `accessRole` 表，并停用使用已废弃 slug（editor/viewer）的账号；权限查询结果带 5 分钟 TTL 内存缓存，修改权限后需调用 `invalidateCache` 或触发 `revokeSessionsForRole` 强制相关用户重新登录。