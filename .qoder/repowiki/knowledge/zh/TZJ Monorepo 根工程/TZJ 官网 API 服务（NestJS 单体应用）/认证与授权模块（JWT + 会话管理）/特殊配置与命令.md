模块依赖环境变量 `JWT_SECRET`、`JWT_ACCESS_TTL`、`JWT_REFRESH_TTL`、`JWT_REFRESH_GRACE_SECONDS`、`LOGIN_MAX_ATTEMPTS`、`LOGIN_LOCK_DURATION_MIN` 控制令牌有效期、轮换宽限期与暴力破解防护阈值；`JwtModule.register({})` 在模块中留空配置，实际签名参数由 `AuthService.issueTokens` 按令牌类型动态指定。