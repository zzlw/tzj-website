NestJS + @nestjs/websockets (Socket.IO) 实现 WebSocket 实时通信；Redis（node_redis）作为在线状态与跨实例广播的后端；JWT（@nestjs/jwt）签发作用域为 `scope=chat` 的短令牌；PostgreSQL pg_trgm 扩展用于消息全文检索；class-validator + class-transformer 做 DTO 校验与类型转换。