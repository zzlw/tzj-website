NestJS + Prisma Client + PostgreSQL；IP 归属地解析使用 `lib-qqwry`（纯真库离线 dat）+ `geoip-lite`（MaxMind GeoLite）+ 在线 API（ip-api.com / ipapi.co）；渠道归类遵循 GA4 默认规则；Swagger 文档通过 `@nestjs/swagger` 注解生成；限流使用 `@nestjs/throttler`。